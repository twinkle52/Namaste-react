const Grocery = () => {
  return (
    <h1>
      Shop online for Grocery! Here Grocery will have all the component same as
      swiggy , or think of flipkart grocery. Working on lazy loading make my
      trip also uses lazy loading, like in header you will see flight , hotels,
      villas these are lazy loading having there own components will be
      displayed when clciked.
    </h1>
  );
};

export default Grocery;
